<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
    </head>
    <body>
        <h1>DUMP DATAS HERE</h1>
        <p><%= value %></p>
        <div>
            <% for (var x = 1; x <= value; x++) { %>
                <p><%= now %> / <%= result[x]; %></p>
            <% } %>
        </div>
    </body>
</html>